<%= form_for(@machine_model) do |f| %>
  <% if @machine_model.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@machine_model.errors.count, "error") %> prohibited this machine_model from being saved:</h2>

      <ul>
      <% @machine_model.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
